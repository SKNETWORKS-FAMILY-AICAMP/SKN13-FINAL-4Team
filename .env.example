# ========================================
# Frontend Environment Variables Example
# ========================================
# 
# 주의: OpenAI API 키는 더 이상 Frontend에서 사용하지 않습니다.
# 보안상 Backend API를 통해 처리됩니다.
#
# 필요한 환경변수는 상위 .env 파일과 Docker Compose에서 관리됩니다.

# MeloTTS Server Configuration (외부 TTS 서버 사용시)
REACT_APP_MELOTTS_SERVER_URL=ws://localhost:8765

# Coqui TTS Server Configuration (외부 TTS 서버 사용시)
REACT_APP_COQUI_SERVER_URL=http://localhost:5002

# 참고: 상위 .env 파일에서 관리되는 환경변수들:
# - OPENAI_API_KEY (Backend 전용)
# - MELOTTS_SERVER_URL (Docker Compose를 통해 REACT_APP_*로 전달)
# - COQUI_SERVER_URL (Docker Compose를 통해 REACT_APP_*로 전달)